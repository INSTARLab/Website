# Claude Code harness state (worktrees, memory, per-project tooling)
.claude/

# Local-only preview/mockup pages (must not publish to gh-pages)
nav-preview.html
issue-triage.html

# Local-only working notes / task list
NOTES.md
